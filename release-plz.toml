[workspace]
# Publishing is entered only through the dispatch-only workflow after a landed
# SHA, exact candidate CI evidence, and an explicit release set pass its gate.
release_always = true
changelog_update = false
git_tag_name = "{{ package }}@{{ version }}"
